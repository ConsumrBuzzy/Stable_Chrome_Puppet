# Chrome Puppet Configuration
# Copy this file to .env and update the values as needed

# ===== Chrome Settings =====

# Chrome binary path (leave empty for auto-detection)
# CHROME_PATH=

# Run Chrome in headless mode (true/false)
CHROME_HEADLESS=false

# Window size (width,height)
CHROME_WINDOW_SIZE=1920,1080

# Default implicit wait time in seconds
CHROME_IMPLICIT_WAIT=10

# Default download directory (absolute path recommended)
DOWNLOAD_DIR=./downloads

# Chrome user data directory (for persistent sessions)
# CHROME_USER_DATA_DIR=./user_data

# ===== Proxy Settings =====

# HTTP_PROXY=http://user:pass@proxy:port
# HTTPS_PROXY=http://user:pass@proxy:port

# ===== User Agent =====

# Custom user agent (optional)
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

# ===== Logging =====

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (set to empty string for console only)
LOG_FILE=chrome_puppet.log

# ===== Advanced Settings =====

# Chrome command line arguments (comma-separated)
# EXTRA_CHROME_ARGS=--disable-gpu,--no-sandbox

# Enable Chrome extensions (comma-separated paths to .crx files or extension directories)
# CHROME_EXTENSIONS=./extensions/ublock.crx,./extensions/extension_dir

# Disable Chrome extensions (comma-separated extension IDs)
# DISABLED_CHROME_EXTENSIONS=cfhdojbkjhnklbpkdaibdccddilifddb

# ===== Performance =====

# Disable GPU hardware acceleration (true/false)
# DISABLE_GPU=false

# Disable extensions (true/false)
# DISABLE_EXTENSIONS=false

# Disable sandbox (Linux only, not recommended unless necessary)
# NO_SANDBOX=false
